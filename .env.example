# Database — matches docker-compose.yml (host port 5433 -> container 5432)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/bank_db"

# Application
PORT=3000
# "development" gives pretty logs; "production" emits structured JSON logs
NODE_ENV=development
# Log level: trace | debug | info | warn | error | fatal
LOG_LEVEL=info
